page.on('dialogue', (dialogue)=> {
    dialogue.accept()
})

page.once()

playwright.config.js

{
    workers: 4
}

npx playwright test --workers=4

project : [
    {name:chromium, use: {
        chromium
    }},{name:firefox},{}.
]


a ->  

let a;

const username = await page.locator();

()[2]

await username.click();

