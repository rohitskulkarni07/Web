converting one type into another


we can convert number, boolean, expression, Date, Array to String
using String() or varible.toString()

we can convert String(must be of numbers), boolean to number using Number(value);

converting unparsables string to number such as 'Hello' will give NaN as output
NaN : Non a Number


use parseInt() for string to integer
use parseFloat() for string to floating point decimal value