DOM Document Object Model

doucment object is inside window Object

DOM can be view as tree consist of nodes created by browser

JS is to use for manipulation of nodesof DOM

DOM elements has its own prperty and methods 