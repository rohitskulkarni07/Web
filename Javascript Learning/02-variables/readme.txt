Single qoutes and double are allowed fro strin

letters, number, _, $ are allowed while decalaring variable

cannot start with number
cannot start with $ only for jquery

use camel case


const variable must be assigned 
you cannot re-assigne const

const object mutation is allowed

const array can be created new values can be added but no new array can be assined