use tag <script> to add js file to html, its kind of linking to html file so that when you run html in browser js will run as code behind.

use console in developer tools in chrome (use ctrl+shift+i) for to see logs